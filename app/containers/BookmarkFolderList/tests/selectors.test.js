import {
  selectBookmarkFolderList,
} from '../selectors';
import { fromJS } from 'immutable';
import expect from 'expect';

const selector = selectBookmarkFolderList();

describe('selectBookmarkFolderList', () => {

});
